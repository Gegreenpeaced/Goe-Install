using System.IO.Enumeration;

class Logger
{

    /*  Logmodes:
        0 = normal
        1 = debug

        Filnames:
        normal name is latest.log
        but it can be changed to specific name for debug reasons
    */
    public static void initLog(int loggingmode = 0, string filename = "")
    {
    }
}